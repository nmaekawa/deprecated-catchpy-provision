---

    - name: copy scripts
      template:
        src: "{{ item }}"
        dest: "{{ hx_rootdir }}/bin/{{ item }}"
        owner: "root"
        group: "root"
        mode: "0776"
      with_fileglob:
        - "../templates/*"
